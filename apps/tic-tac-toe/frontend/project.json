{
  "name": "ttt-frontend",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/tic-tac-toe/frontend",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project ttt-frontend --web",
  "targets": {
    "watch-workers": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p apps/tic-tac-toe/frontend/tsconfig.worker.json --watch"
        ]
      }
    }
  }
}
